version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    working_directory: /blog
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
            - run: npm run build
      - run:
          name: Creating Dummy Artifacts
          command: |
            echo "my artifact file" > /blog/artifact-1;
            mkdir /blog/artifacts;
            echo "my artifact files in a dir" > /blog/artifacts/artifact-2;
workflows:
    build-and-test:
      jobs:
        - build-and-test
